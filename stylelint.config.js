/** @type {import('stylelint').Config} */
export default {
    // Extends the standard config for best practices
    extends: ["stylelint-config-standard"],
    rules: {
        "import-notation": "string"
    },
};
